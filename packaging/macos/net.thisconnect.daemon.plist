<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>net.thisconnect.daemon</string>

	<key>ProgramArguments</key>
	<array>
		<string>/Library/PrivilegedHelperTools/net.thisconnect.daemon</string>
	</array>

	<!-- launchd creates the socket, which removes the bind()/chmod() TOCTOU
	     window and stale-socket cleanup, and allows on-demand launch instead of
	     a resident root process. The daemon picks the descriptor up with
	     launch_activate_socket("Listener"). SPEC.md 7.2. -->
	<key>Sockets</key>
	<dict>
		<key>Listener</key>
		<dict>
			<key>SockFamily</key>
			<string>Unix</string>
			<key>SockType</key>
			<string>Stream</string>
			<key>SockPathName</key>
			<string>/var/run/thisconnect.sock</string>
			<!-- 438 decimal = 0666. Plists are decimal; writing 660 here is a
			     silent footgun. See packaging/README.md for why it is not 0660. -->
			<key>SockPathMode</key>
			<integer>438</integer>
		</dict>
	</dict>

	<key>RunAtLoad</key>
	<false/>
	<key>KeepAlive</key>
	<dict>
		<key>SuccessfulExit</key>
		<false/>
	</dict>

	<key>ProcessType</key>
	<string>Interactive</string>

	<key>EnvironmentVariables</key>
	<dict>
		<!-- Peer authentication cannot resolve the console user without
		     SCDynamicStoreCopyConsoleUser; the installer writes the uid here.
		     Without it the daemon refuses to start rather than listening for
		     everyone. SPEC.md 7.3. -->
		<key>THISCONNECT_ALLOWED_UIDS</key>
		<string>__INSTALLING_UID__</string>
	</dict>

	<key>StandardOutPath</key>
	<string>/var/log/thisconnect/daemon.log</string>
	<key>StandardErrorPath</key>
	<string>/var/log/thisconnect/daemon.log</string>
</dict>
</plist>
